<!-- header section strats -->
    <header class="header_section">
      <div class="container-fluid">
        <nav class="navbar navbar-expand-lg custom_nav-container ">
          <a class="navbar-brand" href="index.html">
            <img src={{asset("images/logo.png")}} alt="">
            <span>
              Petology
            </span>
          </a>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>

          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <div class="d-flex mx-auto flex-column flex-lg-row align-items-center">
              <ul class="navbar-nav">
                <li class="nav-item active">
                  <a class="nav-link" href="{{path('index')}}">Home <span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item">
                  <a class="nav-link" href="{{path('service')}}">service </a>
                </li>
                <li class="nav-item">
                  <a class="nav-link" href="{{path('pet')}}">Pet's gallery </a>
                </li>
                <li class="nav-item">
                  <a class="nav-link" href="{{path('clinic')}}"> clinic</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link" href="{{path('contact')}}">Contact us</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link" href="{{path('buy')}}"> Buy now </a>
                </li>
              </ul>
              <form class="form-inline my-2 my-lg-0 ml-0 ml-lg-4 mb-3 mb-lg-0">
                <button class="btn  my-2 my-sm-0 nav_search-btn" type="submit"></button>
              </form>
            </div>
            <div class="d-flex mx-auto flex-column flex-lg-row align-items-center">
              <ul class="navbar-nav">
              {% if app.user %}
                  <li class="nav-item"><a class="btn btn-primary" href="{{ path('logout') }}"><i class="fa fa-sign-out sr-icons"></i> {{ app.user.name }} - Log out</a></li>
                {% else %}
                  <li class="nav-item {{ (app.request.attributes.get('_route') == 'register')  ? 'active': ''}} "><a class="btn" href="{{ path('register') }}"><i class="fa fa-user-secret sr-icons"></i> Register</a></li>
                  <li class="nav-item {{ (app.request.attributes.get('_route') == 'login')  ? 'active': ''}} lien"><a class="btn btn-primary" href="{{ path('login') }}"><i class="fa fa-sign-in sr-icons"></i> Login</a></li>
                {% endif %}
                </ul>
            </div>
          </div>
        </nav>
      </div>
    </header>